// Import actions

function onConnection() {

}

export default {
  const io = require('socket.io')(8000);
  var socket = io('http://localhost');

  // io.on('connection', function(tweet){
  //   // io.emit('tweet', tweet);
  // });

  return io;
}

export function requestProject(projectKey) {

}
