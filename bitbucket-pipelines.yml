pipelines:
  default:
    - step:
        script:
          - docker login -u $DOCKER_USER -p $DOCKER_PASS
          - docker build -t hackexpert/multus:$BITBUCKET_COMMIT .
          - docker push hackexpert/multus:$BITBUCKET_COMMIT

options:
  docker: true
